void main() {
  // 변수를 만드는 두가지 방법
  // 변수를 수정할 땐 같은 타입으로 해야함
  // 어떤 경우든 변수는 업데이트 가능함
  // var name = '니꼬';
  String name = '니꼬';
  name = 'nico';
  print(name);
  /**
   * 1. 관습적으로 함수나 메소드 내부에 지역 변수를 선언할 때에는 var를 사용
   * 2. class에서 변수나 property를 선언할 때에는 타입을 지정
   */
}
